---
description: Run the planned parallel implementation workflow with choco-pi sub-agents
argument-hint: "<task>"
---
Load and follow the harness `task` skill. Use applicable `AGENTS.md` as repository policy. Treat the following as the user's task: $ARGUMENTS
