---
description: Create one verified local commit from the intended changes
argument-hint: "[message guidance]"
---

Load and follow the harness `commit` skill. Use applicable `AGENTS.md` and repository history as commit policy. Apply this additional user guidance if present: ${ARGUMENTS:-none}.
