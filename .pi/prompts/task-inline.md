---
description: Run the default direct implementation workflow without implementation sub-agents
argument-hint: "<task>"
---

Load and follow the harness `task-inline` skill. Use applicable `AGENTS.md` as repository policy. Treat the following as the user's task: $ARGUMENTS
