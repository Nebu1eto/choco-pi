---
description: Verify the base choco-pi environment and task-scoped capabilities
argument-hint: "[scope or checks]"
---
Load and follow the harness `check` skill. Apply these additional user arguments if present: ${ARGUMENTS:-none}.
