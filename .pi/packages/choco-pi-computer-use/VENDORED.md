# Vendored: choco-pi-computer-use

This directory is a **vendored, renamed fork** of the upstream open-source package **@injaneity/pi-computer-use**. It is not the original repository.

- Original source: https://github.com/injaneity/pi-computer-use
- Base commit: `de72583`
- Base version: `0.5.0`
- Forked on: 2026-08-22
- License: MIT (upstream `LICENSE` copied verbatim)

## What was taken

- `extensions/computer-use.ts`, the TypeScript extension entry.
- All 27 files under `src/`, including the macOS, Linux, and Windows dispatch backends and the shared `helper-path.mjs` module.
- `scripts/setup-helper.mjs`, retained at its original package-relative path for runtime macOS helper installation.
- All four `native/macos/*.swift` files for source hashing and the opt-in `PI_COMPUTER_USE_ALLOW_BUILD=1` fallback.
- The arm64 and x64 loose helper binaries under `prebuilt/macos/` (about 1.1 MB total).
- The upstream MIT `LICENSE` verbatim.

## Divergences

### Package identity and loading

- Renamed `@injaneity/pi-computer-use` to `choco-pi-computer-use`, changed the version to `0.5.0-choco.0`, marked it private, and declared that it supersedes the upstream package.
- Pinned `pi.extensions` to `./extensions/computer-use.ts` instead of scanning the extension directory.
- Removed the npm `postinstall` script. Local harness packages are not installed by npm; helper setup remains runtime-driven.
- Removed the upstream `pi.image` field and all package metadata aimed at publishing the upstream project. Added only the package-local `typecheck` script.
- Kept `typebox` and `@earendil-works/*` as optional host-provided peer dependencies. There are no other third-party runtime dependencies, so this fork has no vendored `node_modules/`.
- Added `tsconfig.json` extending the harness root configuration; there is no build step and no `dist/`.

### Runtime helper installation

- Kept the package-relative runtime installer call. On macOS, `ensureComputerUseSetup` and tool execution reach the current platform's `ensureReady`; `ensureMacosReady` calls `MacosHelperClient.ensureInstalled`, which spawns `scripts/setup-helper.mjs --runtime` only when the resolved helper executable is absent.
- Removed the GitHub-release ZIP fallback from `scripts/setup-helper.mjs`, including release URL/tag constants, network fetches, timeout handling, archive extraction, SHA256SUMS parsing and verification, temporary release files, fallback warnings, and release-specific help text. The fork version would not name an upstream release, and both architecture binaries are committed.
- Retained installation of a pre-signed app bundle when one exists, wrapping of the committed loose macOS binary, Developer ID/local/ad-hoc signing, LaunchServices registration, source-hash tracking, and the opt-in Swift build fallback.
- Corrected the build-fallback gate so runtime invocation alone does not enable Swift compilation. A build now requires `--allow-build` or `PI_COMPUTER_USE_ALLOW_BUILD=1`; upstream also enabled it implicitly for `--runtime`.
- Removed Windows and Linux setup/build branches, paths, Cargo invocation, prebuilt copying, and related help text from the setup script. The script now rejects non-macOS platforms explicitly.

### Platform support and removed trees

- Native helper installation is supported only on macOS in this fork.
- Kept `src/platform/{linux,windows}` because platform dispatch guards those small TypeScript backends, but removed `native/{linux,windows}` and their Rust crates and build scripts. Removed non-macOS prebuilts as well.
- Removed `.github/`, `demo/`, `notes/`, `assets/`, `docs/`, `package-lock.json`, the root upstream `.gitignore`, and upstream project-process/readme files. The fork supplies a concise package `README.md` and `AGENTS.md` that state macOS-only support.
- Removed `scripts/build-native.mjs`, every `scripts/check-*.mjs`, and the remaining development-only scripts and signing assets (`make-signing-cert.sh`, `pi-computer-use.entitlements`, `pi-cubench-agent.mjs`). The package retains only the runtime installer.

### Erasable TypeScript

Node strip-types must load every source file. Three constructors used parameter properties; all were desugared without changing behavior:

- `src/cdp.ts` `CdpTab`: `ws`, `targetId`, and `title` are explicit fields assigned in its private constructor.
- `src/runtime.ts` `StaleResourceStateError`: `resourceKey`, `expectedEpoch`, and `actualEpoch` are explicit readonly fields assigned in the constructor.
- `src/runtime.ts` `StateStore`: `limit` is an explicit private readonly field assigned in the constructor, preserving the default value `128`.

No enums, namespaces, decorators, or other constructor parameter properties remain.

### Anti-slop type hardening

- Hardened the vendored TypeScript and setup script to satisfy the harness anti-slop rules without suppressions. The changes are type-only and behavior-preserving: protocol-owned response types, recursive JSON/accessibility node types, named result types, and boundary predicates replace `unknown`, broad dictionaries, widening assertions, and inline representation checks.
- Preserved all 11 tool names and schemas. Conditional helper request fields are still added only when present; no request field, JSON encoding, command name, response interpretation, or ordering assumption changed.
- The macOS helper IPC path now gives daemon envelopes and command results explicit protocol types. The Linux and Windows backends received compile-time protocol types only and remain dispatch-guarded and unsupported by this fork.
- Permission handling only names the existing `checkPermissions` response and source fields. Accessibility and Screen Recording decisions, attribution handling, prompts, and failure gates are unchanged.
- Helper setup and discovery logic are unchanged. The setup script replaces two representation checks with equivalent primitive/API-presence checks; installation, hash/staleness checks, signing, and LaunchServices registration retain their existing control flow.

### Tests and documentation

- Replaced the upstream source-text `scripts/check-tool-schemas.mjs` check with `tests/load.test.ts`, which imports the extension and bridge under Node strip-types and pins all 11 tool names plus their parameter-property and required-field shapes.
- Made `stateId` optional for the read-only `search_ui`, `expand_ui`, and `inspect_ui` tools. On a fresh operation they now establish a semantic observation internally; this capture disables OCR and images and never delivers input. The `act_ui.stateId` schema is also optional so a missing state reaches its structured runtime refusal, but actions remain state-bound and never auto-observe.
- Defined observation staleness using the existing per-resource scheduler epoch: an action state is stale when its saved epoch differs from the resource's current epoch after a write. Missing and stale action states now throw `ObservationRefreshRequiredError`, whose JSON message and typed `requirement` include `code`, `operation`, `reason`, the requested `stateId` when available, exact `observe_ui` refresh arguments, and both epochs for stale states.
- Added a test-only platform-backend replacement hook so transition tests can exercise full bridge executors without installing or invoking native helpers.
- Added this provenance record, package working rules, and fork-specific README.

### Non-interference policy

The vendored base silently escalated to foreground delivery (app activation, window raise, HID events) for coordinate clicks, editable refs converted to coordinates, and keyboard input without a ref, and retried in foreground after a background `didnt` or a helper `foreground_required` error. This fork removes both paths in TypeScript:

- Foreground delivery requires a host-issued grant: `foreground_grant` (bundle ids, `"*"` allowed, default `[]`) in `pi-computer-use.json` / `.pi/computer-use.json`, or the comma-separated `PI_COMPUTER_USE_FOREGROUND_GRANT`. No tool parameter can set it. `foregroundGrantCovers` in `src/config.ts` is the only check.
- `PreparedAction.wantsForeground` (formerly `needsForeground`/`usesCurrentFocus`) records a capability need and never authorizes anything. Policy is `ax_only` when headless (or when `PI_COMPUTER_USE_DELIVERY_POLICY=ax_only`), otherwise `background`; `foreground` is sent only when `wantsForeground` and the grant covers the target bundle id, and then the request carries `foregroundGrant: true`. `helperActRequest` throws if asked for foreground without a grant. `canRetryInForeground` is deleted.
- Each action is sent to the helper once, with one narrow exception: if a grant covers the target, the policy was `background`, and the helper refused with `foreground_required` and `effectPossible: false` (proof that nothing was delivered), it is retried exactly once as `foreground` with `foregroundGrant: true`. The trace then records `escalatedToForeground: true`, `escalationReason: "foreground_required"`, `backgroundFirst: true`, and `backgroundAttempt`. A `didnt`, a refusal whose effect is possible or unknown, or a refusal without a grant is never retried. Otherwise a helper `foreground_required` error or result, or a background `didnt` for `typeText`/`keypress` or a foreground-wanting action, yields `execution.outcome: "didnt"` and `execution.refusal` (`code`, `target`, `action`, `capability`, `effectPossible`, `grantHint`, `reason`). A transaction stops at the first refusal and reports `stoppedAt`; `act_ui` then returns `details.status: "foreground_required"` without a successor observation.
- `PlatformDeliveryPolicy` no longer includes `"default"`. `PI_COMPUTER_USE_DELIVERY_POLICY`/`PI_COMPUTER_USE_EVENT_DELIVERY` values `foreground`/`hid` are ignored and reported in `details.config.notes`.
- Every `act`/`actBatch` request adds `requestId` (UUID per request), `session: { id, generation }` (per-process id; generation increments on session shutdown), `deadlineMs` (absolute Unix epoch milliseconds), optional `foregroundGrant: true`, and `focusResolution: "ax_focused_element"` for focus targets. `requestId` is the semantic request id that the native `cancel` command will target; it is separate from the transport envelope `id` (`req_N`) that `helper.ts` assigns per socket command. The macOS backend keeps the requested policy for focus targets. The Swift helper source enforces these fields from protocol 7 (next subsection); the committed prebuilt binaries still predate it.
- Helper `frontmostBefore`/`frontmostAfter` response fields pass through to the execution trace when present.
- `tests/foreground-policy.test.ts` pins this behavior with the fake platform backend.

### Protocol 7: cancellation and ownership

`native/macos/bridge.swift` and `src/platform/macos/helper.ts` move to helper protocol 7. The committed `prebuilt/macos/` binaries are not rebuilt by this change and still report protocol 6, so the TypeScript client restarts a protocol-6 daemon and then fails with a mismatch until the prebuilts are replaced.

- **Grant enforcement at the native boundary.** A request's policy is `ax_only`, `background` (also for an absent or legacy `"default"` policy), or `foreground`. Delivery is `hid` only for `policy: "foreground"` with `foregroundGrant: true`; everything else posts by pid. `foreground` without the grant is refused up front with `foreground_required`. App activation, window main/focus/raise (`focusTargetForPhysicalInput`), the activation inside `postEvent`, and the `focusWindow`/`restoreUserFocus` commands run only with the grant; `postEvent` refuses HID delivery when the executing request carries no grant. With `focusResolution: "ax_focused_element"` the helper resolves the target pid's `kAXFocusedUIElementAttribute` (never system focus), delivers by pid, and refuses with `foreground_required` when that element belongs to another pid.
- **`effectPossible`.** Error payloads of `act`/`actBatch` carry `error.effectPossible`: `false` only when the request had not yet posted an event or performed an AX mutation (press, value write, focus set, scroll action). Batch step errors carry it too. `HelperCommandError.effectPossible` exposes it.
- **Frontmost evidence.** `act` and `actBatch` results include `frontmostBefore`/`frontmostAfter` pids.
- **Request identity and cancellation.** `act`/`actBatch` register under their semantic `requestId`s (all batch action ids map to one request). `{cmd:"cancel", target:<requestId>}` answers `{acknowledged:true, state, stoppedAt?, effectPossible?}` with `state` `stopped`, `completed`, `not_found`, or `stopping` (a fourth state the plan did not name: the request was flagged but did not reach a checkpoint within 300 ms). Checkpoints run before each atomic delivery unit (mouse move, each click press, drag start, scroll event, key press, each Unicode text scalar, atomic text), before each AX mutation, before each `actBatch` step, and on each settle-poll iteration; never between the down and up halves of one press, and a started drag always completes. `deadlineMs` (absolute epoch ms) is checked at the same points. A stopped request fails with `{code:"cancelled", effectPossible}` and `result: {outcome:"partial"|"rejected_before_delivery", stoppedAt, reason:"cancelled"|"deadline"}` (batches add `steps`).
- **Ownership.** `claim {session:{id,generation}}` makes that session the owner; the owner lease lasts 30 s from its last request that carried its session. `act`, `actBatch`, `focusWindow`, `setWindowFrame`, `beginInputSuppression`, `endInputSuppression`, and `restoreUserFocus` from any other session, from an older generation of the owner id, or without a session while an owner is live fail with `owned_by_other_session` (`effectPossible:false`) before argument validation. With no live owner, the first gated request with a session becomes the owner. `release` clears ownership for the owner id; `shutdown` is unchanged. Read-only commands are not gated.
- **TypeScript transport.** `daemonCommand` sends `cancel` for the in-flight semantic `requestId` on a second connection when a command is aborted or times out, waits at most 500 ms, and attaches the ack as `cancel` (plus `effectPossible` and any late `response`) to `HelperAbortedError` (message still `Operation aborted.`) or `HelperTransportError`. The client learns the session from act requests and stamps it on owner-gated commands that lack one. `ensureMacosReady` claims ownership after `ensureProtocol` once a session is known (before the first act the helper admits the first act's session); a refused claim throws `owned_by_other_session`. `release()` (behind `releaseMacosOwnership`) forgets the learned session before sending, so a repeated shutdown sends nothing. It never installs, probes, or launches the helper: it sends `release` only when the socket path exists as a socket and the command completes within 500 ms, and returns `false` silently on ENOENT, ECONNREFUSED, or timeout.
- **Bridge mapping.** `act_ui` maps a helper `cancelled` error, or a non-aborted client timeout whose cancel was acknowledged, to `details.status: "cancelled"` with `details.execution` `{outcome:"partial"|"rejected_before_delivery", stoppedAt?, reason, effectPossible, cancel?}`, and `owned_by_other_session` to `details.status: "owned_by_other_session"` with `outcome:"rejected_before_delivery"`. A possibly effective interruption clears the observation so the next action needs `observe_ui`. Caller aborts still rethrow.
- **Tests.** `tests/helpers/fake-helper-daemon.ts` is a wire-level protocol-7 fake (Node `net`, no native access) with scripts `s1-ok` through `s7-ownership`; `tests/helper-transport.test.ts` drives the real client and bridge over it through `PI_CU_SOCKET_PATH`. The fake writes `act`/`actBatch` rows to its `--log` file when answered (with `response` and `answeredAt`), other rows on receipt, and `cancel` rows with their `ack`; its S2/S6 outlines include a focusable `custom-1` "Custom View" node, and S4 holds a `typeText` past the client timeout until cancelled.

## Updating

Diff a fresh checkout of the upstream repository against base commit `de72583`, then reapply every divergence above. Preserve explicit `.ts` relative imports and the one deliberate `.mjs` import. Do not restore npm lifecycle installation, network downloads, or non-macOS native helper support without an explicit fork decision.

## Pi SDK target alignment

Host-provided Pi SDK peer contracts and any development SDK dependencies now
require exactly `0.87.1`, matching the harness target. This SDK alignment is
separate from installer policy; TypeBox and unrelated
dependencies retain their existing contracts.

- **Prebuilt refresh (protocol 7).** `prebuilt/macos/{arm64,x64}/bridge` rebuilt with Apple Swift 6.4 (swiftlang-6.4.0.34.1, macOS SDK 27.0) via `xcrun swiftc -target {arm64,x86_64}-apple-macosx14.0 -module-cache-path <tmp> -O` with the `setup-helper.mjs` frameworks and sources (`agent_cursor.swift`, `agent_cursor_motion.swift`, `bridge.swift`); source revision `9e686eaa` plus uncommitted `native/macos/bridge.swift` (1 file, +393/-48). Loose binaries are linker ad-hoc signed; the installer re-signs the wrapped app. sha256 arm64 `c59601482c77e13fe732b1b5f1422491702295b95d824028492685c156ab350f`, x64 `e38a7089fef1c44e6321e01c35f1dbfc35a34543495cabde739adaae29313ea4` (x64 compiled only, never run; both superseded by the text-field focus refresh below). `releaseMacosOwnership` is now wired into the macOS backend's `shutdown` (`src/platform/index.ts`), best effort and bounded at 2.5 s; this supersedes the "not yet wired" note above.
- **Text-field clicks by pid-scoped AX focus.** A semantic `click`/`press` on a text-input ref with a wire ref now stays a ref target (`src/actions.ts`); only a text input without a wire ref, or a picture-only node, falls back to coordinates, so a ref click never wants foreground. In the helper, a native text role (not inside `AXWebArea`) under policy `background` or `ax_only` first probes `AXUIElementIsAttributeSettable(kAXFocusedAttribute)`; if it is not settable, `background` refuses with `foreground_required` before any mutation (`effectPossible:false`) and `ax_only` keeps the vendored AXPress/pid-coordinate path. Otherwise it sets `kAXFocusedAttribute` (one retry on a re-found element), reports `performed: {delivery:"ax", grounding:"focus", focused:true}`, and the outcome is `worked` when the element reports focused or the app's `kAXFocusedUIElementAttribute` is the element or its descendant (`evidence.focusHeld`), else `didnt` unless the value, selection, or window changed. A failed focus write in `background` refuses with `effectPossible:true`. `AXWebArea` descendants and granted `foreground` keep the prior behavior. Within one transaction on an outline-only look, `typeText`/`keypress` without a ref after such a click target that text-input ref (`ActionState.focusRef`) instead of failing for want of an image focus point. `HelperActPerformed.grounding` in `src/platform/types.ts` does not yet list `"focus"`. The aggregated `act` trace carries `deliveryPolicy` and `delivery` only when every non-wait step agrees. The wire fake answers an ungranted `field-1` click with the same focus result in every script; S2 still refuses the custom view. Prebuilts rebuilt with the same toolchain and recipe (module cache under `/tmp`); `native/macos/bridge.swift` sha256 `a90e9d89d47abf4bc5005a39a0459fcf47a7ebcad4671dac1d0f088ef21f548b`; bridge sha256 arm64 `29024f54654a67ac0e1427e0e0b677a420ec45cf19bcd5b7a2e2518b6220f6af`, x64 `68ce67c319ab5f219a01790aba576ce993fc1aa3ce45c9eebae6df63aa08578b` (x64 compiled only, never run). The arm64 binary answered `diagnostics` with `protocolVersion: 7` and exited on `shutdown`.
- **Review fixes (protocol 7, additive).** Supersedes the earlier statements that a started drag always completes and that `stopping` means the request can deliver no further event.
  - _Keyboard focus writes._ Every `kAXFocusedAttribute` write goes through one helper (`setAXFocus`). Without a foreground grant it allows only control-level focus: an element whose role is readable, is not `AXWindow`, `AXSheet`, `AXDrawer`, `AXApplication`, or `AXSystemWide`, and is not an `AXWebArea` or inside one. Any other target is refused with `foreground_required` before the checkpoint, before `markEffect`, and before any write, so `effectPossible` reflects only earlier work. This applies to `typeText`, `keypress`, the text-field click focus, and `setText` in web content (checked there before activation). As a result, ungranted `typeText`/`keypress` on a window-level or web-content ref, and ungranted web-content `setText`, are now refused. A `focusResolution: "ax_focused_element"` target is already the app's focused element and gets no focus write. Grant-gated window focus (`focusTargetForPhysicalInput`, `focusWindow`, `restoreUserFocus`) uses the same helper.
  - _Drag cancellation._ `postMouseDrag` creates all events before pressing the button, polls cancellation and the deadline before each drag point, and on either releases the button at the last posted point before the checkpoint throws `cancelled` (`partial`). A drag-event failure also releases the button. `stopped` is therefore set only after the mouse-up.
  - _`stopping` is non-terminal._ `cancel` waits up to 300 ms for `stopped`. If the request is still executing, the ack is `stopping` with `effectPossible: true`, and the request answers `cancelled` at its next checkpoint. `HelperInterruption` (`src/platform/types.ts`) gains `state` (a `HelperCancelState` or `refused`) and `reason`. The transport sets `state` to `stopped` only when the helper itself answered `cancelled`; otherwise it uses the ack state. For `stopping`, it forces `effectPossible: true` and sets `reason: "delivery may continue until the helper reports stopped"`. `act_ui` adds `execution.state` and `execution.stateReason`, never reports `stopping` as `rejected_before_delivery`, and words each state's headline separately. Only `stopped` says the helper stopped.
  - _Cancel tombstones._ A `cancel` for an id that is neither active nor recently completed answers `not_found` and records a tombstone (60 s TTL, at most 256 ids, under `requestRegistryLock`). `not_found` means "not yet seen; refused if it arrives". Registration checks tombstones under the same lock, and a matching request starts cancelled. Its first checkpoint runs in `tracked` before the act body, so before look lookup, target resolution, or any AX access. That checkpoint refuses it as `cancelled` with `rejected_before_delivery` and `effectPossible: false`. A flood of more than 256 unknown ids can evict a real tombstone.
  - _Ownership pinning._ Each admitted owner-gated command increments `ownerActiveRequests` under `ownerLock`, and a `defer` decrements it when the command returns; the `defer` is registered only after admission. While the count is positive, the owner neither expires nor can be taken over by another session id, a sessionless request, or another generation of its id. Refusals are `owned_by_other_session` with an additive `error.activeRequests`. While sessionless requests run, a session cannot become owner. An owner's `release` with requests in flight is refused as `owner_busy` (`activeRequests`, `effectPossible: false`); the TypeScript `release()` then returns `false`. Every checkpoint refreshes the owner lease, and the TTL restarts when the request ends. `HelperCommandError.activeRequests` exposes the count.
  - _Lock order._ `ownerLock`, `requestRegistryLock`, each `RequestContext.lock`, and `completedRequestLock` are leaf locks; no path holds two at once. The checkpoint heartbeat runs outside `RequestContext.lock`. `physicalInputLock` (recursive) may be held while a leaf lock is taken, never the reverse.
  - _Fake and tests._ The wire fake models tombstones, pinning (`ownerTtlMs` option), `owner_busy`, `activeRequests`, checkpoint lease refresh, and `stopping`. With the `cancelBlockMs` option, a cancel wakes a held S4 act late. New tests cover the `stopping` interruption and its bridge mapping, cancel-then-act with the same `requestId`, and a claim/release refused while an act is in flight past a 50 ms lease.
  - _Prebuilts._ Rebuilt with Apple Swift 6.4 (swiftlang-6.4.0.34.1) via `xcrun swiftc -target {arm64,x86_64}-apple-macosx14.0 -module-cache-path /tmp/choco-pi/build/modcache -O`, using the `setup-helper.mjs` frameworks and sources. `native/macos/bridge.swift` sha256 `4dfb9155eaf1fa3d13133ec87ea1f0c6dfbf2ee3fb0d39f6dd618e795455909c`; bridge sha256 arm64 `50dbdd9572ea0e6924fbad8c4f1155d3325dec4b3987bbe0329732216da6a0b6`, x64 `0d3c1b9d6614b068cbd088d6fd9d48ac4bc7ed25af5d70aae0b34dc79ea9ae04` (x64 compiled only, never run). On an isolated socket, the arm64 binary answered `diagnostics` with protocol 7. After a `not_found` cancel of id X, it refused an act with `requestId` X as `cancelled`/`rejected_before_delivery` rather than `stale_look`, so the tombstone check ran before the body. It refused a second session's claim with `activeRequests: 0`, released the owner, and exited on `shutdown`.
- **Review fixes, round 2 (protocol 7, additive).** Supersedes the "is not an `AXWebArea` or inside one" wording and the prebuilt hashes in "Review fixes" above.
  - _Fail-closed web ancestry._ `hasAncestorRole` (30-parent cap, `false` on exhaustion or any unreadable parent) is replaced by `ancestorRole`, a tri-state `AncestryResult` (`found`, `notFound`, `inconclusive`) computed by the file-scope `walkAncestry` with a 64-element cap (`ancestryWalkLimit`, the element itself included). Each `AXRole`/`AXParent` read is classified as a value, a proven absence (`kAXErrorNoValue` or `kAXErrorAttributeUnsupported`), or a failure (any other AX error, or a value of the wrong type). `notFound` requires reaching a root: an `AXApplication` element or a proven-absent parent. A failed read or the cap yields `inconclusive`. Without a foreground grant, `axFocusRefusal` allows a focus write only on `notFound`; `inconclusive` is refused with `foreground_required` and the reason "ancestry inconclusive". The refusal still precedes the checkpoint, `markEffect`, and the write (`typeText`, `keypress`, text-field click focus, `setText`). In ungranted `press`/`click`, `inconclusive` is refused right after the role and ancestry reads, before any mutation, for text roles under `background` or `ax_only` and for every target under `background`. `ax_only` non-text targets take the confirmed-web path (AXPress, no focus write). Otherwise `inconclusive` is treated as web content: `setText` takes the web path (`axFocusRefusal` first), and granted clicks use pointer input. The fake daemon cannot model AX depth, so no TypeScript test covers this path. A scratch Swift driver compiled `walkAncestry` verbatim from this source and passed 11 cases: web at depth 31 and 63 is `found`, web at depth 64, a 100-deep native chain, and a parent cycle are `inconclusive`, a parent or ancestor-role read failure is `inconclusive`, and an absent role continues.
  - _Typed test context._ `tests/helpers/extension-context.ts` builds a complete `ExtensionContext` through the host's `ExtensionRunner.createContext()`. It uses an empty in-memory credential store, no models file, no catalog refresh, and an in-memory `SessionManager` (`hasUI` is false). `tests/foreground-policy.test.ts` and `tests/helper-transport.test.ts` use it instead of `as ExtensionContext` casts. `tests/observation-state.test.ts` (committed earlier) still casts.
  - _Prebuilts._ Same toolchain and recipe. `native/macos/bridge.swift` sha256 `8067cffc4ac32e10e9a95a142e487d264cc2f96657653a9e5c03671b43e3418e`; bridge sha256 arm64 `0f8bdf66676d908221dd4898ce9ba39b78fc42d4a6dc6dd257d5f1e0dcbf3964`, x64 `1582ead08a20751c401fe46e4d5bffec4711c928ef57b82bbe33490efffcbc92` (x64 compiled only, never run). On an isolated socket, the arm64 binary answered `diagnostics` with protocol 7 and exited on `shutdown`.

## Tier-B findings (Run B1)

Fixes for the two product findings in `docs/computer-use-e2e-evidence.md` "Run B1". Supersedes the statements above that ungranted `ax_only` text roles without settable focus keep the pid-coordinate path, that S2 refuses every ungranted non-field click, that S6 refuses every ungranted non-setText action, and that a granted S6 custom-view click answers `worked`.

- **No-AX-action clicks are refused before delivery (native).** In `act`, a `click`/`press` whose element has no `AXPress`, is not handled by the text-role AX focus path, and is not confirmed or inconclusive web content now throws `foreground_required` ("target has no accessibility action; pointer delivery needs a host foreground grant") unless the request is a granted `foreground` request. The refusal runs before any checkpoint, `markEffect`, AX write, or event, so `effectPossible` is `false` unless earlier work in the same request (an `ax_only` text focus write that failed, or an earlier batch step) marked an effect. This applies to `background` and `ax_only`. A granted `foreground` request takes the HID path as before. Elements with `AXPress` still use AXPress, including its vendored pid fallback when AXPress fails. Text roles still use AX focus, and web descendants keep their prior behavior, including the `ax_only` pid pointer path.
- **Outcome of a pid click on an element without an AX action (native).** After such a click, `act` polls a bounded settle window (6 x 50 ms, with a checkpoint on each iteration) for a change in the element's value or selection, the app's focused window, the sheet count, the onscreen CG window ids, or the frontmost app. It reports `worked` if it sees one; otherwise it reports `didnt` with `evidence.observableChange: false`. A non-empty root delta (non-batch requests) upgrades `didnt` to `worked`. With the refusal above, this path runs only for a granted request with `params.delivery: "pid"` or an `ax_only` web-content target. `unknown` remains only where the helper cannot evaluate the effect: HID delivery; a coordinate target without an element; a successful AXPress, or a pid click after a failed AXPress, that leaves no AX-visible change; and non-click coordinate actions.
- **Window-frame staleness (native).** `LookRecord` now stores the observed root AX element and its AX frame. A scoped refresh with `baseLookId` inherits both from its base look, as it already inherits the coordinate geometry. Right after the initial checkpoint, before target resolution or any delivery, `act` compares the element's current frame with the recorded one. A difference of more than 1 pt in origin or size, or a frame that can no longer be read, throws `stale_look` ("Window frame changed since look <id>; observe again") with `effectPossible` from the request context (`false` for a single `act`). Ref-based actions, including AX value writes, are not exempt. There is no polling or timer. A look whose frame was unreadable skips the check, and so do synthesized `cgmenu:` roots. Every `actBatch` step runs the check, so a window that one step moves or resizes refuses the later steps.
- **TypeScript.** `PreparedAction` click/press gains `pointerOnly` (a ref target whose node has neither `canPress` nor `isTextInput`). `refusalForResult` maps a background `didnt` on such a click to `foreground_required` (`effectPossible: true`, capability `pointer_input`), in headless `ax_only` as well. A `didnt` is never retried. The single granted retry still requires `foreground_required` with `effectPossible: false`. After a granted attempt, the grant hint no longer says that the background attempt was not retried. An action is marked "changed (acted here)" in the window note only when the whole execution ended `worked` (after any postcondition or value check) or its own step outcome is `worked`. Otherwise its region gets "effect not verified", and the `act_ui` text adds "Effect not verified for action N (<action>: <outcome>)". `details.execution.steps[].outcome` is unchanged. `HelperActPerformed` declares `verification: "caller_required"`.
- **Fake daemon and tests.** In every script the wire fake refuses an ungranted click on a ref with no AX press action that is not a text input (`custom-1`) before delivery, and presses `button-1` through AX with or without a grant. A granted HID click on `custom-1` activates and answers `unknown`, and a granted `params.delivery: "pid"` click answers `didnt`. Looks record the window frame. `fixtureMutation: "resize"` on a `look` moves and resizes the window to `RESIZED_FRAME`, and a later act with an earlier look id is refused with `stale_look`. S5's generation bump is unchanged. The new foreground-policy tests cover a no-action ref click that is refused once, the same refusal with a grant that escalates once to a granted foreground act, a `didnt` on such a click that is refused and not retried, and an `unknown` that makes no change claim, surfaces the step outcome, and leaves `worked` still marked "acted here". The new transport tests cover a resize refused as `stale_look` with no delivery, the granted pid `didnt`, and the updated S2/S6 expectations. The suite has 65 tests.
- **Prebuilts.** Built with Apple Swift 6.4 (swiftlang-6.4.0.34.1) via `xcrun swiftc -target {arm64,x86_64}-apple-macosx14.0 -module-cache-path /tmp/choco-pi/build/modcache -O`, using the `setup-helper.mjs` frameworks and sources. Source revision `86aa83d9` plus uncommitted `native/macos/bridge.swift`. `native/macos/bridge.swift` sha256 `d8bc07452222d66e347f28568ba76ab146014472fbfda2af4b3b6440326147d3`; bridge sha256 arm64 `a07277cc77ff5834ed8d7d196e24d55e5a3c2cf488eb8cb763179e583ceeb348`, x64 `2659a42cc5b2c180a8b027cc7a3f8d91be02a259b465be26c22290b755c24ccd` (x64 compiled only, never run). On an isolated socket, the arm64 binary answered `diagnostics` with protocol 7 and exited on `shutdown`. The refusal, settle, and frame paths need a real target window and were not exercised natively.
