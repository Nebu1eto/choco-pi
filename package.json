{
  "name": "choco-pi-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:profile": "node scripts/install-profile.mjs",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "test": "node --test tests/*.test.ts .pi/packages/*/tests/*.test.ts",
    "typecheck": "tsc --noEmit && node scripts/typecheck-packages.mjs"
  },
  "devDependencies": {
    "@earendil-works/pi-agent-core": "0.84.3",
    "@earendil-works/pi-ai": "0.84.3",
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@earendil-works/pi-tui": "0.84.3",
    "@oxlint/plugins": "1.79.0",
    "@types/node": "^26.0.0",
    "grok-mermaid": "0.2.2",
    "oxfmt": "0.64.0",
    "oxlint": "1.79.0",
    "typebox": "^1.3.12",
    "typescript": "^7.0.2"
  }
}
